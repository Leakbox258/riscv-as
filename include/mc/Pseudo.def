/// reference https://www.cs.sfu.ca/~ashriram/Courses/CS295/assets/notebooks/RISCV/RISCV_CARD.pdf

PSEUDO(LA, ( auipc rd %hi ) ( addi rd rd %lo ) )
PSEUDO(LD, ( auipc rd %hi ) ( ld rd rd %lo ) ) // conflict
PSEUDO(SD, ( auipc rt %hi ) ( sd rd rt %lo ) )	// conflict
PSEUDO(FLD, ( auipc rt %hi ) ( fld rd rt %lo ) ) // conflict
PSEUDO(FSD, ( auipc rt %hi ) ( fsd rd rt %lo ) ) // conflict

PSEUDO(NOP, ( addi x0, x0, 0 ) )
// PSEUDO(LI)
PSEUDO(MV, ( addi rd rs 0 ) )
PSEUDO(NOT, ( xori rd rs -1 ) )
PSEUDO(NEG, ( sub rd x0 rs ) )
PSEUDO(NEGW, ( subw rd x0 rs ) )
PSEUDO(SEXT_W, ( addiw rd rs 0 ) )
PSEUDO(SEQZ, ( sltiu rd rs 1 ) )
PSEUDO(SNEZ, ( sltu rd x0 rs ) )
PSEUDO(SLTZ, ( slt rd rs x0 ) )
PSEUDO(SGTZ, ( slt rd x0 rs ) )

PSEUDO(FMV_S, ( fsgnj.s rd rs rs ) )
PSEUDO(FABS_S, ( fsgnjx.s rd rs rs ) )
PSEUDO(FNEG_S, ( fsgnjn.s rd rs rs ) )
PSEUDO(FMV_D, ( fsgnj.d rd rs rs ) )
PSEUDO(FABS_D, ( fsgnjx.d rd rs rs ) )
PSEUDO(FNEG_D, ( fsgnjn.d rd rs rs ) )

PSEUDO(BEQZ, ( beq rs x0 offset ) )
PSEUDO(BNEZ, ( bne rs x0 offset ) )
PSEUDO(BLEZ, ( bge x0 rs offset ) )
PSEUDO(BGEZ, ( bge rs x0 offset ) )
PSEUDO(BLTZ, ( blt rs x0 offset ) )
PSEUDO(BGTZ, ( blt x0 rs offset ) )

PSEUDO(BGT, ( blt rt rs offset ) )
PSEUDO(BLE, ( bge rt rs offset ) )
PSEUDO(BGTU, ( bltu rt rs offset ) )
PSEUDO(BLEU, ( bgeu rt rs offset ) )

PSEUDO(J, ( jal x0 offset ) )
PSEUDO(JAL, ( jal x1 offset )) // conflict
PSEUDO(JR, ( jalr x0 rs 0 ) )
PSEUDO(JALR, ( jalr x1 rs 0 ) ) // conflict
PSEUDO(RET, ( jalr x0 x1 0 ) )
PSEUDO(CALL, ( auipc x1 %hi ) ( jalr x1 x1 %lo ) )
PSEUDO(TAIL, ( auipc x6 %hi ) ( jalr x0 x6 %lo ) )
